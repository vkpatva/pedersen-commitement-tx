[package]
name = "pedersen-commitment-tx"
version = "0.1.0"
edition = "2021"
description = "Educational demo: confidential transactions with Pedersen commitments (toy integers, not production crypto)"

[dependencies]
